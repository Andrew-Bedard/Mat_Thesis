function [bool_value, stage_number] = 



for i = 1:length(abrevs)
    stage = abrevs{i};
    for j = 1:length(abrevs{i})
    k = strfind(name, stage(j));
    if isempty(k) == false
        stage(j)
    end
end
end